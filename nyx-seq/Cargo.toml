[package]
name = "nyx-seq"
version = "0.1.0"
edition = "2024"

[dependencies]
nyx-core = { path = "../nyx-core", default-features = false }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
