[package]
name = "nyx-core"
version = "0.1.0"
edition = "2024"

[features]
default = ["audio"]
audio = ["dep:cpal"]

[dependencies]
cpal = { version = "0.17", optional = true }
rtrb = "0.3"
spectrum-analyzer = "1.6"
