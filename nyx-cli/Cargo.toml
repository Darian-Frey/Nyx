[package]
name = "nyx-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
nyx-core = { path = "../nyx-core" }
nyx-seq = { path = "../nyx-seq" }
notify = "7"
libloading = "0.8"
clap = { version = "4", features = ["derive"] }
